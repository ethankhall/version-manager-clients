{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "description": "A listing of tokens for the repo.",
  "javaName": "RepoTokenList",
  "properties": {
    "tokens": {
      "type": "array",
      "description": "List of tokens.",
      "items": {
        "$ref" : "token-description.json"
      },
      "id": "tokens"
    }
  },
  "id": "/",
  "required": [
    "tokens"
  ]
}