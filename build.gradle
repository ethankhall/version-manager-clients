buildscript {
    repositories {
        mavenCentral()
        mavenLocal()
        maven {
            url 'http://repo.ehdev.io/maven2'
        }
    }
    dependencies {
        classpath 'io.ehdev.conrad.client:gradle-client:0.0.2'
    }
}

apply plugin: 'version-manager'

allprojects {
    group = 'io.ehdev.conrad.client'
}

versionManager {
    projectName = 'ethankhall'
    repoName = 'version-manager-clients'
}